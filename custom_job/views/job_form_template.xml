<odoo>
  <template id="job_form_template" name="Job Form">
    <t t-call="website.layout">
      <div class="container mt-5 mb-5">
        <h2 class="mb-4 text-center text-primary">Post a Job Opening</h2>

        <!-- Alert Box -->
        <div id="alertBox" style="display: none; padding: 10px; margin-bottom: 20px; border-radius: 4px;"></div>

        <form id="jobForm" style="display: flex; flex-wrap: wrap; gap: 20px;">
          <!-- Form Fields -->
          <t t-foreach="[
            ['job_id', 'Job ID', 'text', true],
            ['job_title', 'Job Title', 'text', true],
            ['experience', 'Experience', 'text', false],
            ['shift', 'Shift', 'text', false],
            ['company', 'Company', 'text', false],
            ['location', 'Location', 'text', false],
            ['salary', 'Salary', 'text', false],
            ['skills', 'Skills', 'text', false]
          ]" t-as="field">
            <div style="flex: 1 1 45%;">
              <label for="${field[0]}">${field[1]} <t t-if="field[3]"><span style="color: red;">*</span></t></label><br/>
              <input id="${field[0]}" type="${field[2]}" name="${field[0]}" t-att-required="field[3] ? 'required' : None" style="width: 100%;" />
            </div>
          </t>

          <!-- Status -->
          <div style="flex: 1 1 45%;">
            <label for="status">Status</label><br/>
            <select id="status" name="status" style="width: 100%;">
              <option value="active">Active</option>
              <option value="inactive">Inactive</option>
            </select>
          </div>

          <!-- Workplace Type -->
          <div style="flex: 1 1 45%;">
            <label for="workplace_type">Workplace Type</label><br/>
            <select id="workplace_type" name="workplace_type" style="width: 100%;">
              <option value="onsite">On-site</option>
              <option value="remote">Remote</option>
              <option value="hybrid">Hybrid</option>
            </select>
          </div>

          <!-- Dates -->
          <div style="flex: 1 1 45%;">
            <label for="posted_date">Posted Date</label><br/>
            <input id="posted_date" type="date" name="posted_date" style="width: 100%;" />
          </div>

          <div style="flex: 1 1 45%;">
            <label for="joining_tentative_date">Joining Date</label><br/>
            <input id="joining_tentative_date" type="date" name="joining_tentative_date" style="width: 100%;" />
          </div>

          <!-- Responsibilities -->
          <div style="flex: 1 1 100%;">
            <label for="responsibilities">Responsibilities</label><br/>
            <textarea id="responsibilities" name="responsibilities" rows="3" style="width: 100%;"></textarea>
          </div>

          <!-- Requirements -->
          <div style="flex: 1 1 100%;">
            <label for="requirement">Requirements</label><br/>
            <textarea id="requirement" name="requirement" rows="3" style="width: 100%;"></textarea>
          </div>

          <!-- Submit Button -->
          <div style="flex: 1 1 100%; text-align: center; margin-top: 20px;">
            <button type="submit" style="padding: 12px 40px; background-color: #007bff; color: white; border: none; border-radius: 4px; cursor: pointer;">
              Submit Job
            </button>
          </div>
        </form>
      </div>

      <script>
        document.addEventListener('DOMContentLoaded', function () {
          const form = document.getElementById('jobForm');
          const alertBox = document.getElementById('alertBox');

          form.addEventListener('submit', async function (e) {
            e.preventDefault();

            const formData = new FormData(form);
            const data = {};
            formData.forEach((value, key) => {
              data[key] = value;
            });

            try {
              const response = await fetch("/api/jobs", {
                method: "POST",
                headers: {
                  "Content-Type": "application/json"
                },
                body: JSON.stringify(data)
              });

              const result = await response.json();

              if (response.status === 201) {
                showAlert("✅ " + result.message, "green");
                form.reset();
              } else {
                showAlert("❌ " + (result.error || "Something went wrong"), "red");
              }
            } catch (error) {
              showAlert("❌ Request failed: " + error.message, "red");
            }
          });

          function showAlert(message, color) {
            alertBox.innerText = message;
            alertBox.style.backgroundColor = color;
            alertBox.style.color = "white";
            alertBox.style.display = "block";
            alertBox.scrollIntoView({ behavior: "smooth" });
          }
        });
      </script>
    </t>
  </template>
</odoo>
